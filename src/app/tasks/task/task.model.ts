export interface TaskProps {
  id: string;
  userId: string;
  title: string;
  summary: string;
  dueDate: string;
}

export interface newTaskProps {
  title: string;
  summary: string;
  dueDate: string;
}